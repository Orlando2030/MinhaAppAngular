import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

@NgModule({
  declarations: [
    // Seus componetes
  ],
  imports: [
    CommonModule
    // Seus moduloes
  ],
  providers:[
    // Seus serviso, nessa versão do angular não esta vindo o  providers:[
  ]
})
export class FuncionalidadeModule { }
